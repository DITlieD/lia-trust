# Programs / funding log (L7)

L7 applications happen **only after** an L6-tagged release is maintained.
Rows below are placeholders for dates; no unmeasured product numbers may appear
in applications (claims-lint clean).

| Date | Program | Status | Notes |
|------|---------|--------|-------|
| — | (none filed) | TRACKED | Wait for L6 tag + maintenance window |

Rule: any numeric claim in an application must appear in `docs/claims.json`
with `[MEASURED]` or `[EXTERNAL]`.
